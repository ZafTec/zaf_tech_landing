---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

interface Props {
  aboutImage: ImageMetadata;
}

const { aboutImage } = Astro.props;
---

<section id="about" class="py-16">
  <div class="mx-auto grid max-w-6xl items-center gap-10 px-4 lg:grid-cols-[1fr,1.1fr]">
    <!-- <div class="overflow-hidden rounded-4xl bg-surface shadow-card">
      <Image
        src={aboutImage}
        alt="Design and strategy planning session"
        width={1200}
        height={900}
        widths={[480, 768, 960, 1200]}
        format="webp"
        quality={70}
        loading="lazy"
        decoding="async"
        class="h-full w-full object-cover"
        sizes="(min-width: 1024px) 45vw, 100vw"
      />
    </div> -->
    <div class="space-y-4">
      <p class="text-sm font-semibold text-accent">About us</p>
      <h2 class="text-3xl font-semibold">Engineering teams that ship production systems.</h2>
      <p class="text-ink-muted">
        We combine deep technical expertise with product thinking. Every engagement
        includes senior engineers, iterative delivery, and maintainable architecture.
      </p>
      <div class="grid gap-3 sm:grid-cols-2">
        {[
          "UI/UX Design",
          "Backend Engineering",
          "Cloud Infrastructure",
          "Machine Learning",
        ].map((item: string) => (
          <div class="rounded-2xl bg-surface px-4 py-3 text-sm font-semibold shadow-card">
            {item}
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
